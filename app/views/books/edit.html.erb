<h1>編集画面</h1>
<!-- form_with部分 -->
<%= form_with model:@book, local: true do |f| %>

<p>booktaitoru：<%= f.text_field :title %></p>
<p>booksetumei：<%= f.text_field :body %></p>

<%= f.submit '保存' %>

<%= link_to "show", book_path(@book.id) %>
<%= link_to "back", books_path %>

<% end %>y 